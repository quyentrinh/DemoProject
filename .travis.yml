language: objective-c
osx_image: xcode12.5
script:
  - set -o pipefail
  - xcodebuild -workspace DemoProject.xcworkspace -scheme DemoProject-Dev -destination "platform=iOS Simulator,OS=14.5,name=iPhone 12 Pro" build test | xcpretty
before_install:
  - bundle install
  - bundle exec pod install --repo-update



